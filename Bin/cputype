#!/bin/sh
#
# Compare to ../Util/platform.sml
#
# If you change this file to recognize your
# system, then change Platform.cputype too.

case `uname -s` in
SunOS) echo sparc ;;
OSF1) echo alpha ;;
Linux) echo talx86 ;;
*) echo unsupported ;;
esac
