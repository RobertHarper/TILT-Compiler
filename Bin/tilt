#!/bin/sh
bindir=`dirname "$0"`
cputype=`"$bindir"/cputype`
case $cputype in
unsupported)
	echo 1>&2 "tilt: unsupported architecture; try tilt-nj"
	exit 1
	;;
*)
	TILTROOT=`cd "$bindir"/..; pwd`
	export TILTROOT
	exec "$bindir"/$cputype/tilt ${1+"$@"}
	;;
esac
